<%= form_with(model: cuenca, local: true) do |form| %>
  <% if cuenca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cuenca.errors.count, "error") %> prohibited this cuenca from being saved:</h2>

      <ul>
      <% cuenca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_vertiente %>
    <%= form.number_field :id_vertiente, id: :cuenca_id_vertiente %>
  </div>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :cuenca_nombre %>
  </div>

  <div class="field">
    <%= form.label :id_status %>
    <%= form.number_field :id_status, id: :cuenca_id_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
