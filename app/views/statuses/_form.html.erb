<%= form_with(model: status, local: true) do |form| %>
  <% if status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :status_nombre %>
  </div>

  <div class="field">
    <%= form.label :visible %>
    <%= form.number_field :visible, id: :status_visible %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
